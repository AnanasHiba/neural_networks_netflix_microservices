
server:
  port: 8888
spring:
  application:
    name: "config"
  eureka:
    client:
      healthcheck:
        enabled: true
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: native
  security:
    user:
      password: "root1234"